<!DOCTYPE html>
<html lang={{ page.lang }}>

<head>
  <meta charset="{{ meta.charset }}">
  <title>{{ page.title }}</title>
  <meta name="description" content="{{ meta.description }}">
</head>

<body>
  {{#each notes}}
  <p>{{ this.text }}</p>
  {{/each}}
</body>

</html>
